{
    "scripts": {
        "build": "bun build.js",
        "deploy": "bash deploy.sh",
        "dev": "bunx live-server src/ --no-browser --open='/'  --entry-file='index.html'"
    },
    "devDependencies": {
        "html-minifier-terser": "^7.2.0",
        "lightningcss": "^1.32.0"
    }
}
